# Base URL of the backend API.
# Leave empty to use relative paths (recommended: the Vite dev server proxies
# /api and /healthz to http://localhost:8000, and in production the FastAPI
# backend serves the built frontend from the same origin).
VITE_API_URL=
