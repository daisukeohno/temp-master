# Base URL for the backend API.
# Leave empty to use the same origin (default when the SPA is served by FastAPI).
# Set to a full URL (e.g. https://temp-master.fly.dev) when running the frontend
# dev server against a remote backend.
VITE_API_URL=
