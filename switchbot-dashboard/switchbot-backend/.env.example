# SwitchBot API Credentials
# Get these from the SwitchBot app:
# 1. Go to Profile > Preferences > About
# 2. Tap App Version 10 times to enable Developer Options
# 3. Go to Developer Options > Get Token

SWITCHBOT_TOKEN=your_token_here
SWITCHBOT_SECRET=your_secret_here

# API key that protects the sensitive endpoints
# (/api/backup, /api/import, /api/meters/refresh).
# REQUIRED: if unset, those endpoints return HTTP 503 (fail closed).
# Generate a strong random value, e.g. `openssl rand -hex 32`.
# Clients must send it via the `X-API-Key` header
# (or `Authorization: Bearer <key>`).
DASHBOARD_API_KEY=your_dashboard_api_key_here

# Comma-separated list of origins allowed by CORS.
# Defaults to https://temp-master.fly.dev when unset.
ALLOWED_ORIGINS=https://temp-master.fly.dev
